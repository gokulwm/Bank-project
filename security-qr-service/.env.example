SECURITY_SVC_PORT=8105
SECURITY_SVC_SESSION_DIR=/run/security-qr-service/sessions
# Production: use a root-owned tmpfs-mounted key file instead.
# SECURITY_SVC_KEY_FILE=/run/security-qr-service/signing-key
# Development only: generate a random value and provide its Base64 encoding.
# SECURITY_SVC_KEY_B64=
